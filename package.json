{
  "name": "atlantis-world-magical-keys",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ maxschnaider/atlantis-world-magical-keys",
    "create-local": "graph create --node http://localhost:8020/ maxschnaider/atlantis-world-magical-keys",
    "remove-local": "graph remove --node http://localhost:8020/ maxschnaider/atlantis-world-magical-keys",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 maxschnaider/atlantis-world-magical-keys"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
